Password recover request for your {{ application.name }} account.

Someone requested a password recover for your user.
If it was not you ignore this email, otherwise follow the instructions below.

Please copy & paste the following link in your browser, it will expire en 30 minutes.
[{{ application.config.frontend_url ?: frontend_url }}/recover/{{ user.id }}?code={{ user.recover_password_code }}]


Cheers,
{{ application.name }}