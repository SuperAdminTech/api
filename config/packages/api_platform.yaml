api_platform:
    title: 'AUTH REST API'
    description: 'https://api.auth.dfnder.com'
    version: 1.0.0
    mapping:
        paths: ['%kernel.project_dir%/src/Entity', '%kernel.project_dir%/src/Dto']
    patch_formats:
        json: ['application/merge-patch+json']

    collection:
        pagination:
            client_items_per_page: true
            maximum_items_per_page: 100
    swagger:
        versions: [3]
        api_keys:
            apiKey:
                name: Authorization
                type: header
